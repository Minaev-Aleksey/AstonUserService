spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888  # Адрес Config Server
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/